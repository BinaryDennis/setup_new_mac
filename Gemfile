# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'cocoapods', :path => '/usr/local/bin'
gem 'fastlane', :path => '/usr/local/bin'
